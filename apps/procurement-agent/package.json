{
  "name": "@b2b-starter/procurement-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Procurement agent app stub — populated in C.0.",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@b2b-starter/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
